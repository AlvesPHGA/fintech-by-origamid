import { tv } from 'tailwind-variants';

export const sidenavStyle = tv({
   base: 'bg-color-3 rounded-[1.25rem] text-center pt-5 pb-5 h-fit lap:px-10',
});

export const logoStyle = tv({
   base: 'block text-6xl',
});
